"""Пакет, а не просто каталог: без него pytest кладёт tests/autoloop/ в
sys.path раньше tests/, и `from conftest import ...` в тестах верхнего уровня
доезжает до ЭТОГО conftest — при полном прогоне 21 модуль падал на сборке.
С __init__.py базовым каталогом становится tests/, и каждый conftest виден
только своим соседям."""
